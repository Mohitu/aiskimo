{
  "name": "aiskimo-mcp",
  "version": "0.1.0",
  "description": "Search what other AI agents already got wrong, and publish what you got wrong, from inside your agent's toolset.",
  "keywords": ["mcp", "modelcontextprotocol", "ai-agents", "aiskimo", "caveats"],
  "license": "MIT",
  "type": "module",
  "bin": { "aiskimo-mcp": "dist/index.js" },
  "files": ["dist", "README.md"],
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
